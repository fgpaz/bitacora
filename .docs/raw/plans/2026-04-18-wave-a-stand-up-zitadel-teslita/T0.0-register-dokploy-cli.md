# Task T0.0: Registrar dokploy-cli en CLAUDE.md

## Shared Context
**Goal:** habilitar a todas las waves siguientes a invocar `dkp.sh` sin re-descubrir el skill.
**Stack:** documentacion project policy.
**Architecture:** CLAUDE.md es source-of-truth para policy de skills en el repo; hoy no menciona `dokploy-cli`.

## Locked Decisions
- El bloque de registro se agrega al final de la seccion "Registered Subagent Catalog" o en una seccion dedicada "Runtime tooling" si conviene mantenerlo alejado de agents.
- El wrapper actual vive en `$HOME/.claude/skills/dokploy-cli/scripts/dkp.sh` (confirmado en sesion: doctor corre bien sin API key; falla con error explicito al ejecutar comandos reales).
- El repo usa env vars desde `infra/.env` para `DOKPLOY_API_KEY` y `DOKPLOY_URL`.
- NO tocar `AGENTS.md` (no existe en el repo — solo CLAUDE.md).

## Task Metadata
```yaml
id: T0.0
depends_on: []
agent_type: ps-worker
files:
  - modify: CLAUDE.md  # append block at end
complexity: low
done_when: "Grep 'dokploy-cli' CLAUDE.md count >= 1 and git commit local creado"
```

## Reference
`$HOME/.claude/skills/dokploy-cli/SKILL.md` — seccion "Project registration (first use)" define el bloque exacto a pegar.

## Prompt
1. Abrir `C:\repos\mios\humor\CLAUDE.md` y localizar el final del archivo (despues de la seccion `## 11) Alignment Rule`).
2. Agregar una seccion nueva `## 12) Runtime tooling` **solo si no existe**. Dentro, pegar el bloque exacto definido en el template abajo, sustituyendo `~/.agents/skills/` por `~/.claude/skills/` porque este repo usa la instalacion canonica bajo `.claude`.
3. NO modificar ninguna otra seccion de CLAUDE.md.
4. Commitear con el mensaje del `Commit` section.

### Template exacto a pegar
```md
## 12) Runtime tooling

### dokploy-cli
- Script: `~/.claude/skills/dokploy-cli/scripts/dkp.ps1` (Windows) or `scripts/dkp.sh` (POSIX)
- Auth: `DOKPLOY_API_KEY` + `DOKPLOY_URL` in `infra/.env` (searched upward from CWD)
- Override: set `DOKPLOY_API_KEY` and `DOKPLOY_URL` directly in environment
- Header: `x-api-key` (NOT `Authorization: Bearer`)
- Verify: `bash ~/.claude/skills/dokploy-cli/scripts/dkp.sh doctor`

### mi-key-cli (mkey)
- Script: `~/.claude/skills/mi-key-cli/scripts/mkey.ps1` (Windows) or `scripts/mkey.sh` (POSIX)
- Project config: `infra/mkey.yaml` (vault `teslita`, project `bitacora`, default env `prod`)
- Verify: `bash ~/.claude/skills/mi-key-cli/scripts/mkey.sh doctor`
- Pull secrets: `bash ~/.claude/skills/mi-key-cli/scripts/mkey.sh pull bitacora prod`
- Set secret: `bash ~/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora <KEY> <VALUE> --env prod`
```

## Execution Procedure
1. Read `C:\repos\mios\humor\CLAUDE.md` (full file) para confirmar que no contiene "dokploy-cli" ni "mi-key-cli" aun. Si alguno esta presente, stop y reporta al dispatcher.
2. Edit: append la seccion `## 12) Runtime tooling` al final del archivo usando Edit tool con `old_string` = las ultimas lineas existentes + "\n" y `new_string` = las mismas lineas + el bloque nuevo. **No usar Write (reemplaza file completo).**
3. Validar: `grep -c 'dokploy-cli' CLAUDE.md` debe devolver >= 1. `grep -c 'mi-key-cli' CLAUDE.md` debe devolver >= 1.
4. `git add CLAUDE.md && git commit -m "docs(policy): register dokploy-cli and mi-key-cli tooling in CLAUDE.md"`
5. Stop y reporta si: el file no existe, ya contiene la seccion `## 12)`, o el commit falla por pre-commit hook.

## Skeleton
```md
(fin del CLAUDE.md previo)

## 12) Runtime tooling

### dokploy-cli
- Script: `~/.claude/skills/dokploy-cli/scripts/dkp.ps1` (Windows) or `scripts/dkp.sh` (POSIX)
...
```

## Verify
`grep -E "^## 12\) Runtime tooling|dokploy-cli|mi-key-cli" CLAUDE.md` produce >= 3 lineas matching.

## Commit
`docs(policy): register dokploy-cli and mi-key-cli tooling in CLAUDE.md`
