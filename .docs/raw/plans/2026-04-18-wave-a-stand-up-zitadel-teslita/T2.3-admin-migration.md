# Task T2.3: Setup admin@nuestrascuentitas.com + enroll MFA TOTP

## Shared Context
**Goal:** crear user admin `admin@nuestrascuentitas.com` en Zitadel, asignarle rol IAM Owner, enrollar MFA TOTP, desactivar el admin transitorio anterior si existia.
**Stack:** Zitadel Management API + TOTP enrollment flow.
**Architecture:** admin@ es pre-requisito de T2.5 (MFA forzada); hay que enrolarla antes de aplicar policy para no auto-bloquearse.

## Locked Decisions
- Email: `admin@nuestrascuentitas.com`. Requiere mailbox listo (T0.1 sub-step 4).
- MFA: TOTP (Authenticator app) desde dia 1.
- Rol: IAM Owner (acceso admin a todos los Orgs y projects).
- Admin previo (ej. `buhosoft70@gmail.com` si se uso de bootstrap): NO borrar sin confirmacion — solo desactivar state.

## Task Metadata
```yaml
id: T2.3
depends_on: [T2.2]
agent_type: ps-worker
files:
  - create: .docs/raw/reports/2026-04-18-wave-a-audit/T2.3-admin-migration.md
complexity: high
done_when: |
  (1) user admin@nuestrascuentitas.com existe con state=active, type=human;
  (2) tiene IAM Owner role asignado;
  (3) GET /management/v1/users/{id}/auth-factors/_search devuelve 1 TOTP enrolled;
  (4) admin previo en state=inactive (no deleted)
```

## Reference
- POST `/management/v1/users/human/_import`
- PUT `/admin/v1/members/<userId>` (grant IAM Owner)
- POST `/management/v1/users/{userId}/otp/_set` (enroll TOTP)
- Zitadel TOTP: https://zitadel.com/docs/guides/manage/user/reg-create-user#otp

## Prompt
1. `eval $(mkey pull bitacora prod --stdout | grep -E '^ZITADEL_')`.
2. Crear user:
   ```bash
   curl -sS -X POST -H "Authorization: Bearer $ZITADEL_ADMIN_PAT" -H "Content-Type: application/json" \
     -d '{
       "userName": "admin@nuestrascuentitas.com",
       "profile": {"firstName":"Admin","lastName":"Teslita","displayName":"Admin Teslita","preferredLanguage":"es"},
       "email": {"email":"admin@nuestrascuentitas.com","isEmailVerified":false},
       "password":"<tmp-password>",
       "passwordChangeRequired": true
     }' \
     https://id.nuestrascuentitas.com/management/v1/users/human/_import
   ```
   Capturar `userId`.
3. Verificar email: Zitadel envia mail de verificacion (via SMTP wireado en T2.2). El user abre mailbox y click en link.
4. **Pedir al user que inicie sesion y enrolle TOTP**:
   - Abrir `https://id.nuestrascuentitas.com/ui/login`, loguear con admin@nuestrascuentitas.com + tmp-password.
   - Zitadel pide cambiar password. Nueva password guardar en Infisical: `mkey set bitacora ZITADEL_ADMIN_PASSWORD "<new>" --env prod`.
   - Settings > Authenticators > Add Authenticator > Authenticator App. Escanear QR con Google/Microsoft/Aegis Authenticator.
   - Guardar TOTP recovery codes en Infisical: `mkey set bitacora ZITADEL_ADMIN_TOTP_RECOVERY "<codes>" --env prod`.
5. Asignar IAM Owner role:
   ```bash
   curl -sS -X POST -H "Authorization: Bearer $ZITADEL_ADMIN_PAT" -H "Content-Type: application/json" \
     -d "{\"userId\":\"$NEW_ADMIN_USER_ID\",\"roles\":[\"IAM_OWNER\"]}" \
     https://id.nuestrascuentitas.com/admin/v1/members
   ```
6. Validar MFA enrollment:
   ```bash
   curl -sS -X POST -H "Authorization: Bearer $ZITADEL_ADMIN_PAT" -H "Content-Type: application/json" \
     -d "{}" \
     "https://id.nuestrascuentitas.com/management/v1/users/$NEW_ADMIN_USER_ID/auth-factors/_search" | jq '.result[] | select(.otp)'
   ```
7. Desactivar (NO borrar) admin anterior:
   ```bash
   curl -sS -X POST -H "Authorization: Bearer $ZITADEL_ADMIN_PAT" \
     "https://id.nuestrascuentitas.com/management/v1/users/$OLD_ADMIN_USER_ID/_deactivate"
   ```
   Donde `$OLD_ADMIN_USER_ID` salio de T1.2 users list.
8. **Generar PAT nuevo para el admin migrado** y rotar el anterior:
   - Login como admin nuevo en UI.
   - Service Users > Personal Access Tokens > Add. Copiar el nuevo PAT.
   - `mkey set bitacora ZITADEL_ADMIN_PAT "<new-pat>" --env prod` (sobre-escribir el viejo).
   - Revocar PAT anterior desde UI.
9. Persistir reporte con userIds y timestamps.

## Skeleton
```md
# T2.3 — Admin migration + MFA

## Admin nuevo
- Email: admin@nuestrascuentitas.com
- userId: `<id>`
- State: active
- MFA TOTP: enrolled (1 factor)
- IAM Owner role: granted

## Admin anterior (si existia)
- Email: `<email>`
- userId: `<id>`
- State: inactive (deactivated, not deleted)

## PAT rotation
- PAT anterior revocado: yes
- PAT nuevo en Infisical: yes

## Credenciales en Infisical
- ZITADEL_ADMIN_PAT: yes (rotado)
- ZITADEL_ADMIN_EMAIL: admin@nuestrascuentitas.com
- ZITADEL_ADMIN_PASSWORD: yes
- ZITADEL_ADMIN_TOTP_RECOVERY: yes
```

## Verify
`mkey pull bitacora prod --stdout | grep -c "^ZITADEL_ADMIN_" >= 4 && curl -sS -H "Authorization: Bearer $PAT_NEW" https://id.nuestrascuentitas.com/management/v1/orgs/me | jq -e '.org.id'` exits 0.

## Commit
`docs(report): T2.3 admin migration + mfa totp enrolled`
