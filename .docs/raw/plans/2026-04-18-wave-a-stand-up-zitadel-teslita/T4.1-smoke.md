# Task T4.1: Smoke test firmado Wave A + evidencia completa

## Shared Context
**Goal:** ejecutar smoke test end-to-end del Zitadel wireado y persistir evidencia en `artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/`. Sin este artifact, Wave A no cierra.
**Stack:** curl + jq + bash, screenshots de admin UI, headers mail.
**Architecture:** evidencia demuestra que (1) OIDC discovery OK, (2) JWKS valido, (3) client credentials grant funciona, (4) SMTP envia con DKIM/SPF, (5) backup corrio, (6) orgs presentes en UI.

## Locked Decisions
- Artifact path: `artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/`.
- Formato README.md firmado + evidencia cruda adjunta.
- Todo JWT mostrado con `sub` masked (`<redacted>`).
- Todo mail muestra headers completos excepto `X-Auth-Results` con email final masked.

## Task Metadata
```yaml
id: T4.1
depends_on: [T3.1, T3.2, T3.3, T3.4, T3.5]
agent_type: ps-worker
files:
  - create: artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/README.md
  - create: artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/01-oidc-discovery.json
  - create: artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/02-jwks.json
  - create: artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/03-client-credentials-grant.md
  - create: artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/04-smtp-test-mail-headers.md
  - create: artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/05-backup-evidence.md
  - create: artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/06-orgs-screenshot.png
complexity: medium
done_when: "README.md firmado con 6 bloques de evidencia, todos GREEN"
```

## Reference
- Wave A prompt original (seccion "Smoke test firmado")
- T2.2 report (SMTP), T2.4 report (clients), T3.1 report (backup)

## Prompt
1. Cargar PAT + client creds test:
   ```bash
   eval $(mkey pull bitacora prod --stdout | grep -E '^ZITADEL_(ADMIN_PAT|CLIENT_BITACORA_API_)')
   ```
2. **Evidencia 1 — OIDC Discovery:**
   ```bash
   curl -sS https://id.nuestrascuentitas.com/.well-known/openid-configuration \
     -o artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/01-oidc-discovery.json
   jq '{issuer, jwks_uri, authorization_endpoint, token_endpoint, id_token_signing_alg_values_supported}' \
     artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/01-oidc-discovery.json
   ```
   Debe tener `issuer=https://id.nuestrascuentitas.com`, `RS256` en signing algs.
3. **Evidencia 2 — JWKS:**
   ```bash
   curl -sS https://id.nuestrascuentitas.com/oauth/v2/keys \
     -o artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/02-jwks.json
   jq '.keys[] | {kid, kty, use, alg}' artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/02-jwks.json
   ```
   Debe mostrar al menos 1 key con `alg=RS256`.
4. **Evidencia 3 — Client Credentials grant:**
   ```bash
   TOKEN=$(curl -sS -X POST https://id.nuestrascuentitas.com/oauth/v2/token \
     -d "grant_type=client_credentials" \
     -d "scope=openid urn:zitadel:iam:org:project:id:$ZITADEL_PROJECT_BITACORA_ID:aud" \
     -u "$ZITADEL_CLIENT_BITACORA_API_ID:$ZITADEL_CLIENT_BITACORA_API_SECRET" | jq -r '.access_token')
   # Decode header + payload (masked sub)
   echo "$TOKEN" | cut -d. -f1 | base64 -d 2>/dev/null | jq
   echo "$TOKEN" | cut -d. -f2 | base64 -d 2>/dev/null | jq '.sub = "<redacted>"'
   ```
   Guardar ambos outputs en `03-client-credentials-grant.md` con el JWT header mostrando `alg=RS256, kid=<id>`.
5. **Evidencia 4 — Test mail SMTP:**
   - Disparar mail verification a buhosoft70@ (ya hecho en T2.2).
   - Pedir al user que copie los headers del mail recibido y pegarlos en `04-smtp-test-mail-headers.md`.
   - Verificar: `Received-SPF: pass`, `dkim=pass`, sender `<RESEND_SENDER_EMAIL>`.
6. **Evidencia 5 — Backup:**
   ```bash
   sshr exec --host turismo --cmd "ls -la /var/backups/zitadel/ | head -5"
   rclone ls <provider>:<bucket>/ | grep zitadel-pg- | head -5
   ```
   Copiar outputs a `05-backup-evidence.md` con timestamps y file sizes.
7. **Evidencia 6 — Screenshot Orgs:**
   - Pedir al user que abra https://id.nuestrascuentitas.com/ui/console y capture la vista de Organizations mostrando las 4.
   - Guardar como `06-orgs-screenshot.png`.
   - Alternative: si no hay screenshot, usar `curl` con PAT y listar orgs en JSON, guardar como `06-orgs.json`.
8. **README firmado:**
   Generar `artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/README.md`:
   ```md
   # Wave A Smoke Test — Zitadel Teslita

   **Fecha:** 2026-04-18
   **Operador:** paz.fgabriel@gmail.com
   **Issue tracking:** #16

   ## Resumen
   | Check | Estado | Evidencia |
   |-------|--------|-----------|
   | OIDC Discovery 200 | GREEN | 01-oidc-discovery.json |
   | JWKS valido RS256 | GREEN | 02-jwks.json |
   | Client credentials grant | GREEN | 03-client-credentials-grant.md |
   | Test mail DKIM+SPF pass | GREEN/AMBER | 04-smtp-test-mail-headers.md |
   | Primer backup offsite | GREEN | 05-backup-evidence.md |
   | 4 Orgs creadas y visibles | GREEN | 06-orgs-screenshot.png |

   ## Detalle

   ### 1. OIDC Discovery
   - Issuer: `https://id.nuestrascuentitas.com`
   - JWKS URI: `https://id.nuestrascuentitas.com/oauth/v2/keys`
   - Signing algs: `RS256, ES256, EdDSA`

   ### 2. JWKS
   - Keys disponibles: N
   - Primera key: `kid=<id>, alg=RS256, use=sig`

   ### 3. Client credentials grant (bitacora API)
   - Client ID: `<masked>`
   - Token type: `Bearer`
   - Expires in: `3600s`
   - JWT header: `{"alg":"RS256","kid":"<kid>"}`
   - JWT payload masked: `{"iss":"https://id.nuestrascuentitas.com","sub":"<redacted>","aud":["<project-id>"],"exp":...,"iat":...}`

   ### 4. SMTP test mail
   - From: noreply@nuestrascuentitas.com
   - To: buhosoft70@gmail.com (masked en headers)
   - Received-SPF: pass
   - DKIM-Signature: pass (d=nuestrascuentitas.com, s=resend)
   - Delivery latency: `<ms>`

   ### 5. Backup primero
   - Local path: `/var/backups/zitadel/zitadel-pg-<ts>.tar.gz`
   - File size: `<MB>`
   - Offsite: `<provider>:<bucket>/zitadel-pg-<ts>.tar.gz`
   - Cron instalado: yes, siguiente ejecucion 03:00 UTC

   ### 6. Organizations
   - nuestrascuentitas: active
   - bitacora: active
   - multi-tedi: active
   - gastos: active

   ## Gaps / Issues abiertos
   - <lista — ej: DKIM alineamiento fino, backup provider final, mailbox admin@, etc.>

   ## Firma
   Wave A completada por Claude Code + Gabriel Paz, 2026-04-18.
   Desbloqueada: Wave B (#17).
   ```
9. Commit.

## Verify
`test -f artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/README.md && grep -c GREEN artifacts/e2e/2026-04-18-zitadel-wave-a-smoke/README.md` >= 5.

## Commit
`docs(smoke): T4.1 wave a zitadel smoke test firmado con evidencia`
