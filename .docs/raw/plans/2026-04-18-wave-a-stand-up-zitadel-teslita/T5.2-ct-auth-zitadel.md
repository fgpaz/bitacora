# Task T5.2: Crear 09_contratos/CT-AUTH-ZITADEL.md

## Shared Context
**Goal:** crear el contrato tecnico de Zitadel Teslita como referencia para Wave B y siguientes integraciones.
**Stack:** Markdown spec doc.
**Architecture:** existe `CT-AUTH.md` (Supabase actual). Este doc es el **paralelo para Zitadel**, no reemplaza a `CT-AUTH.md`.

## Locked Decisions
- Path: `.docs/wiki/09_contratos/CT-AUTH-ZITADEL.md`.
- Estructura: endpoints OIDC, claims, JWKS, PAT scopes, OAuth Clients mapping, policies baseline, SMTP wiring, transicion desde Supabase (Wave B), escape hatches.
- Linked desde `09_contratos_tecnicos.md` (agregar entrada en indice).

## Task Metadata
```yaml
id: T5.2
depends_on: [T4.1]
agent_type: ps-docs
files:
  - create: .docs/wiki/09_contratos/CT-AUTH-ZITADEL.md
  - modify: .docs/wiki/09_contratos_tecnicos.md  # add index entry
complexity: medium
done_when: "CT-AUTH-ZITADEL.md tiene >=8 secciones y es referenciado desde 09_contratos_tecnicos.md"
```

## Reference
- `09_contratos/CT-AUTH.md` — patron estilo
- `01-oidc-discovery.json` artifact smoke para endpoints reales
- T2.4 report — OAuth clients

## Prompt
Generar `09_contratos/CT-AUTH-ZITADEL.md` con el siguiente contenido (rellenar `<>` con valores reales de artifacts smoke):

```md
# CT-AUTH-ZITADEL: Contrato IdP Zitadel Teslita

> Root: `09_contratos_tecnicos.md` — seccion Autenticacion.
> Sibling contract: `CT-AUTH.md` (Supabase Auth, runtime actual Bitacora Wave A).
> Status: Wave A completada 2026-04-18. Wave B integra Bitacora — ver plan Wave B.

## 1. Instancia

| Campo | Valor |
|-------|-------|
| Producto | Zitadel self-hosted v<VERSION> |
| Licencia | Apache 2.0 |
| Host | `id.nuestrascuentitas.com` |
| VPS | turismo (`54.37.157.93`) |
| Postgres | dedicada (no comparte con Bitacora) |
| TLS | Let's Encrypt via Dokploy/Traefik |
| Ecosistema | Teslita (nuestrascuentitas, bitacora, multi-tedi, gastos) |

## 2. Endpoints OIDC (validados smoke T4.1)

| Endpoint | URL |
|----------|-----|
| issuer | `https://id.nuestrascuentitas.com` |
| authorization | `https://id.nuestrascuentitas.com/oauth/v2/authorize` |
| token | `https://id.nuestrascuentitas.com/oauth/v2/token` |
| userinfo | `https://id.nuestrascuentitas.com/oidc/v1/userinfo` |
| jwks_uri | `https://id.nuestrascuentitas.com/oauth/v2/keys` |
| introspection | `https://id.nuestrascuentitas.com/oauth/v2/introspect` |
| revocation | `https://id.nuestrascuentitas.com/oauth/v2/revoke` |
| end_session | `https://id.nuestrascuentitas.com/oidc/v1/end_session` |
| device_authorization | `https://id.nuestrascuentitas.com/oauth/v2/device_authorization` |

Signing algs soportados: `RS256, RS384, RS512, ES256, ES384, ES512, EdDSA`. Default: `RS256`.

## 3. JWT claims emitidos

| Claim | Descripcion |
|-------|-------------|
| sub | stable user id (zitadel userId) |
| aud | `<projectId>` al que pertenece el client |
| iss | `https://id.nuestrascuentitas.com` |
| exp/iat | timestamps UTC |
| email | verified email |
| email_verified | boolean |
| preferred_username | displayName |
| name / given_name / family_name | perfil |
| locale | idioma |
| urn:zitadel:iam:org:id | orgId del user |
| urn:zitadel:iam:org:project:roles | map {role: {orgId: orgName}} |

## 4. Organizations

| Name | orgId | Productos |
|------|-------|-----------|
| nuestrascuentitas | `<id>` | auth central landing/marketing |
| bitacora | `<id>` | Bitacora clinical mood tracker |
| multi-tedi | `<id>` | multi-tedi (futuro) |
| gastos | `<id>` | gastos (futuro) |

## 5. OAuth Clients (por org, con redirect URIs placeholder)

### bitacora
- projectId: `<id>`
- Web App (PKCE public): `<clientId>`, redirects `https://bitacora.nuestrascuentitas.com/auth/callback`, `http://localhost:3000/auth/callback`
- API App (client_credentials): `<clientId>` + secret en Infisical `ZITADEL_CLIENT_BITACORA_API_SECRET`

### nuestrascuentitas / multi-tedi / gastos
Idem estructura, redirect URIs en T2.4 report.

**Los redirect URIs son placeholders Wave A**; cada Wave de migracion los actualiza con URIs reales.

## 6. Policies baseline Teslita (aplicadas T2.5)

| Policy | Valor |
|--------|-------|
| Password min length | 10 |
| Password complexity | upper + lower + number |
| Lockout max password attempts | 10 |
| Lockout max OTP attempts | 10 |
| MFA OTP requerida | yes (factor obligatorio) |
| MFA forzada admins | yes (IAM_OWNER, ORG_OWNER) |
| MFA end users | opcional (grace period 30d) |
| Session access lifetime | 1h |
| Session refresh lifetime | 30d |
| Audit retention | 2 anos (17520h) |

## 7. SMTP (Resend)

| Campo | Valor |
|-------|-------|
| Provider | Resend |
| Host:Port | smtp.resend.com:587 |
| TLS | STARTTLS |
| Sender | noreply@nuestrascuentitas.com |
| DKIM | validado smoke T4.1 |
| SPF | validado smoke T4.1 |

## 8. Admin access

- Admin canonical: `admin@nuestrascuentitas.com` (MFA TOTP enrolled)
- IAM_OWNER role assigned
- PAT rotation: cada 90d (manual por ahora, automatizar Wave D)
- Recovery: TOTP recovery codes en Infisical key `ZITADEL_ADMIN_TOTP_RECOVERY`

## 9. Transicion desde Supabase (Wave B, planificada)

Bitacora hoy (Wave A) valida JWT Supabase via clave simetrica (`SUPABASE_JWT_SECRET`). Wave B introduce validacion JWKS RS256 de Zitadel en paralelo (dual-IdP feature flag). Mapeo de claims:

| Supabase claim | Zitadel claim | Handling |
|---------------|---------------|----------|
| sub | sub | directo |
| email | email | directo |
| user_metadata.role | urn:zitadel:iam:org:project:roles | parsing custom |
| aud | aud | diferente formato, accept both |
| iat/exp | iat/exp | directo |

Plan: `.docs/raw/plans/2026-04-17-migracion-bitacora-zitadel-wave-b.md`.

## 10. Backup y disaster recovery

- Backup strategy: docker volume snapshot + offsite (Wave A T3.1)
- Cron: 03:00 UTC diario
- Offsite provider: `<provider final>` (ver Infisical `ZITADEL_BACKUP_OFFSITE_*`)
- Retention: 30d local / 90d offsite
- Runbook: `infra/runbooks/zitadel-backup.md`
- Recovery runbook: `infra/runbooks/zitadel-recovery.md`

## 11. Escape hatches

- **Perdida de masterkey:** destructive, requiere destroy-and-recreate
- **Rate-limit Let's Encrypt:** LE staging hasta resolver root cause
- **SMTP down:** Zitadel funciona sin SMTP (sin mails de verification/reset); solo para emergencias
- **VPS turismo down:** IdP ecosistema offline; Supabase Bitacora independiente sigue funcionando

## Sincronizacion

Cambios en este contrato fuerzan revision de:
- `09_contratos_tecnicos.md` (indice)
- Wave B plan (ajusta mapeo claims, dual-IdP transicion)
- `07_baseline_tecnica.md` (auth runtime dual)
- `02_arquitectura.md` (diagram Auth externa)
- Memoria `project_idp_zitadel_multi_ecosistema.md` (endpoints)
```

### Update `09_contratos_tecnicos.md`
En la seccion Autenticacion (cerca de linea :16 `> Detalle: CT-AUTH.md`), agregar:
```
> Detalle adicional IdP ecosistema: `09_contratos/CT-AUTH-ZITADEL.md` (Wave A completada 2026-04-18).
```

## Verify
`test -f .docs/wiki/09_contratos/CT-AUTH-ZITADEL.md && grep -c CT-AUTH-ZITADEL .docs/wiki/09_contratos_tecnicos.md` >= 1.

## Commit
`docs(wiki): T5.2 create CT-AUTH-ZITADEL technical contract`
