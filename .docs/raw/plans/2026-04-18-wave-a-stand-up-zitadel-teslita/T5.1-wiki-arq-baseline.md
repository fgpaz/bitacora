# Task T5.1: Update wiki 02_arquitectura.md + 07_baseline_tecnica.md (dual IdP transicion)

## Shared Context
**Goal:** reflejar en el wiki que Zitadel Teslita esta operativo al lado de Supabase Auth, con la anotacion de que Wave B migra Bitacora a Zitadel.
**Stack:** Markdown wiki.
**Architecture:** no borramos la seccion Supabase (sigue siendo auth de Bitacora). Agregamos seccion Zitadel paralela con estado "Wave A completada", endpoints reales, y nota de "Wave B en progreso".

## Locked Decisions
- Supabase Auth queda documentado como auth activo de Bitacora en Wave A.
- Zitadel documentado como IdP del ecosistema Teslita operativo.
- Transicion a Zitadel como auth de Bitacora queda referenciada a Wave B (`humor:.docs/raw/plans/2026-04-17-migracion-bitacora-zitadel-wave-b.md`).
- No borrar ni refactorear contenido existente — solo append + ajustes minimos de contexto.

## Task Metadata
```yaml
id: T5.1
depends_on: [T4.1]
agent_type: ps-docs
files:
  - modify: .docs/wiki/02_arquitectura.md
  - modify: .docs/wiki/07_baseline_tecnica.md
complexity: medium
done_when: "Ambos docs mencionan Zitadel Teslita como IdP ecosistema activo con fecha Wave A"
```

## Reference
- `02_arquitectura.md:66-79` (stack tecnologico) y `:40-43` (Auth externa diagram)
- `07_baseline_tecnica.md` seccion Pipeline / Auth

## Prompt
### Cambio en `02_arquitectura.md`
1. En la seccion "Vista general del sistema" (mermaid), agregar dentro de `subgraph ExtAuth["Auth externa"]` un nodo para Zitadel:
   ```mermaid
   subgraph ExtAuth["Auth externa"]
       SUPA["Supabase Auth (Wave A, auth Bitacora)<br/>auth.bitacora.nuestrascuentitas.com"]
       ZITA["Zitadel Teslita (Wave A completada 2026-04-18, IdP ecosistema)<br/>id.nuestrascuentitas.com"]
   end
   ```
   Mantener la flecha AUTH --> SUPA. Agregar nota que Wave B cambia AUTH --> ZITA.
2. En la tabla "Stack tecnologico" (`:68-79`) agregar fila:
   | Capa | Tecnologia | Justificacion |
   | Auth ecosistema (futuro) | Zitadel v<X> self-hosted | Wave A ok 2026-04-18, OIDC + PKCE, multi-org, MFA forzada admins. Wave B migra Bitacora. Detalle: `09_contratos/CT-AUTH-ZITADEL.md`. |
3. En "Responsabilidades por modulo" (`:80-91`), en la fila Auth, agregar:
   > Nota 2026-04-18: Zitadel Teslita (`id.nuestrascuentitas.com`) esta activo como IdP del ecosistema. Wave B planifica la migracion de Bitacora de Supabase a Zitadel con mapeo 1:1 de claims y fallback dual-IdP.

### Cambio en `07_baseline_tecnica.md`
1. Despues de la tabla "Middleware pipeline" (`:43-53`), agregar nueva seccion:
   ```md
   ## Auth runtime (Wave A dual IdP)

   | Activo | Provider | Dominio | Role |
   |--------|----------|---------|------|
   | Bitacora runtime (Wave A) | Supabase Auth GoTrue v2.177.0 | auth.bitacora.nuestrascuentitas.com | valida JWT con clave simetrica SUPABASE_JWT_SECRET |
   | Ecosistema Teslita (Wave A completada 2026-04-18) | Zitadel v<X> self-hosted | id.nuestrascuentitas.com | OIDC + JWKS RS256, MFA forzada admins, 4 Orgs: nuestrascuentitas / bitacora / multi-tedi / gastos |

   Wave B migra el backend Bitacora de Supabase a Zitadel (feature flag dual-IdP transitional). Detalle: `09_contratos/CT-AUTH-ZITADEL.md`, runbooks `infra/runbooks/zitadel-*.md`.
   ```
2. En "Runtime hardening" (`:76-92`), agregar fila:
   > T3-AUTH-IDP-01: Zitadel Teslita live en `id.nuestrascuentitas.com`; validado 2026-04-18 via `curl https://id.nuestrascuentitas.com/.well-known/openid-configuration` = 200.

## Execution Procedure
1. Leer los 2 docs, identificar anchors exactos.
2. Edit puntual (no Write completo).
3. Validar: `grep -c Zitadel .docs/wiki/02_arquitectura.md` >= 3, `grep -c Zitadel .docs/wiki/07_baseline_tecnica.md` >= 2.
4. Commit.

## Verify
`grep -c "id.nuestrascuentitas.com" .docs/wiki/02_arquitectura.md .docs/wiki/07_baseline_tecnica.md` total >= 3.

## Commit
`docs(wiki): T5.1 add zitadel teslita idp to arquitectura + baseline`
