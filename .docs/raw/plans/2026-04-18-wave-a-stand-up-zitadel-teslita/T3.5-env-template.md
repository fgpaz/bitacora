# Task T3.5: Update infra/.env.template con keys ZITADEL_* + DOKPLOY_*

## Shared Context
**Goal:** actualizar `infra/.env.template` agregando la superficie de env vars Zitadel (reference para onboarding) sin romper la estructura actual.
**Stack:** dotenv template.
**Architecture:** `.env.template` es **git-tracked**, describe shape del `.env` local generado por `mkey pull`. Nunca contiene valores reales.

## Locked Decisions
- Agregar seccion `# --- Zitadel Teslita IdP (Wave A) ---` despues de la seccion Supabase.
- Comentarios explicativos por key (que hace, quien la setea, si es sensible).
- No borrar keys existentes. Solo append.

## Task Metadata
```yaml
id: T3.5
depends_on: [T2.5]
agent_type: ps-worker
files:
  - modify: infra/.env.template
complexity: low
done_when: "infra/.env.template tiene >= 25 entradas con prefijo ZITADEL_* y las DOKPLOY_* ya declaradas quedan documentadas"
```

## Reference
- `infra/.env.template` actual
- T3.3 `infra/dokploy/zitadel/env.example` (fuente de keys)

## Prompt
1. Leer `infra/.env.template` actual y confirmar donde termina la seccion Supabase.
2. Usar Edit para appendear al final del archivo el bloque siguiente:

```
# --- Zitadel Teslita IdP (Wave A, 2026-04-18) ---
# Todas estas keys viven en Infisical vault `teslita` proj `bitacora` env `prod`.
# Ver: infra/dokploy/zitadel/env.example para la config completa.
# Ver: infra/runbooks/zitadel-bootstrap.md para bootstrap from scratch.

# Version pinneada
ZITADEL_VERSION=
ZITADEL_POSTGRES_VERSION=

# Core runtime
ZITADEL_MASTERKEY=            # CRITICAL: never rotate without re-encrypting DB
ZITADEL_EXTERNALDOMAIN=id.nuestrascuentitas.com
ZITADEL_EXTERNALSECURE=true
ZITADEL_EXTERNALPORT=443

# Database dedicada (NO reutilizar BITACORA_DB_*)
ZITADEL_DATABASE_POSTGRES_HOST=
ZITADEL_DATABASE_POSTGRES_PORT=5432
ZITADEL_DATABASE_POSTGRES_DATABASE=zitadel
ZITADEL_DATABASE_POSTGRES_USER=zitadel
ZITADEL_DATABASE_POSTGRES_PASSWORD=

# Dokploy app IDs (no secretos, trazabilidad)
ZITADEL_DOKPLOY_APP_ID_WEB=
ZITADEL_DOKPLOY_APP_ID_POSTGRES=
ZITADEL_POSTGRES_VOLUME_NAME=
ZITADEL_POSTGRES_CONTAINER_NAME=
ZITADEL_DOCKER_NETWORK_NAME=

# SMTP Resend (comparte con RESEND_* pero prefijado para trazabilidad)
ZITADEL_SMTP_HOST=smtp.resend.com
ZITADEL_SMTP_PORT=587
ZITADEL_SMTP_USER=resend
ZITADEL_SMTP_PASSWORD=
ZITADEL_SMTP_SENDER_EMAIL=
ZITADEL_SMTP_SENDER_NAME=

# Admin (Wave A T2.3, MFA forzada)
ZITADEL_ADMIN_EMAIL=paz.fgabriel@gmail.com
ZITADEL_ADMIN_PASSWORD=
ZITADEL_ADMIN_PAT=
ZITADEL_ADMIN_TOTP_RECOVERY=

# OAuth Clients por app (Wave A T2.4, redirect URIs placeholder — cada Wave actualiza)
# --- nuestrascuentitas ---
ZITADEL_PROJECT_NUESTRASCUENTITAS_ID=
ZITADEL_CLIENT_NUESTRASCUENTITAS_WEB_ID=
ZITADEL_CLIENT_NUESTRASCUENTITAS_API_ID=
ZITADEL_CLIENT_NUESTRASCUENTITAS_API_SECRET=
# --- bitacora ---
ZITADEL_PROJECT_BITACORA_ID=
ZITADEL_CLIENT_BITACORA_WEB_ID=
ZITADEL_CLIENT_BITACORA_API_ID=
ZITADEL_CLIENT_BITACORA_API_SECRET=
# --- multi-tedi ---
ZITADEL_PROJECT_MULTI_TEDI_ID=
ZITADEL_CLIENT_MULTI_TEDI_WEB_ID=
ZITADEL_CLIENT_MULTI_TEDI_API_ID=
ZITADEL_CLIENT_MULTI_TEDI_API_SECRET=
# --- gastos ---
ZITADEL_PROJECT_GASTOS_ID=
ZITADEL_CLIENT_GASTOS_WEB_ID=
ZITADEL_CLIENT_GASTOS_API_ID=
ZITADEL_CLIENT_GASTOS_API_SECRET=

# Backup offsite (Wave A T3.1)
ZITADEL_BACKUP_OFFSITE_PROVIDER=b2
ZITADEL_BACKUP_OFFSITE_BUCKET=
ZITADEL_BACKUP_OFFSITE_ACCESS_KEY=
ZITADEL_BACKUP_OFFSITE_SECRET_KEY=
ZITADEL_BACKUP_OFFSITE_ENDPOINT=
```

3. Validar que el template sigue siendo parseable:
   ```bash
   awk -F= '/^[A-Z][A-Z0-9_]*=/{print $1}' infra/.env.template | sort -u | wc -l
   # Debe ser >= 40 (antes ~32, +25 ZITADEL_ nuevas con overlap permitido)
   ```
4. Commit.

## Verify
`grep -c '^ZITADEL_' infra/.env.template` >= 25.

## Commit
`feat(env): T3.5 document zitadel env vars in infra/.env.template`
