# Task T3.3: Retro-generar compose + traefik labels + env.example git-tracked

## Shared Context
**Goal:** crear la carpeta `infra/dokploy/zitadel/` con `compose.yml`, `env.example`, y docs Traefik que **reflejen exactamente el runtime actual** descubierto en T1.1. Permite rebuild deterministico si hay que recrear.
**Stack:** docker-compose v3, Traefik labels para Dokploy managed proxy.
**Architecture:** Dokploy maneja sus propios compose managed. Nuestro compose.yml es **para referencia y disaster recovery**, no para que Dokploy lo deploy directamente (salvo que el user lo migre). Debe ser reproducible.

## Locked Decisions
- Version pinneada a la tag real descubierta en T1.1 (nunca `:latest`).
- Postgres version + env vars copiadas del runtime.
- Traefik labels replicados del runtime.
- `env.example` con nombres de keys + comentarios (sin valores reales).
- No `docker-compose up` — este artefacto es referencia.

## Task Metadata
```yaml
id: T3.3
depends_on: [T2.5]
agent_type: ps-worker
files:
  - create: infra/dokploy/zitadel/compose.yml
  - create: infra/dokploy/zitadel/env.example
  - create: infra/dokploy/zitadel/traefik.labels.md
  - create: infra/dokploy/zitadel/README.md
  - create: .docs/raw/reports/2026-04-18-wave-a-audit/T3.3-compose-retro.md
complexity: medium
done_when: "4 archivos creados, compose valida con docker compose -f compose.yml config sin errores"
```

## Reference
- T1.1 report — runtime exacto
- https://zitadel.com/docs/self-hosting/deploy/compose

## Prompt
1. Leer T1.1 report para extraer: version Zitadel, version Postgres, env vars, volumes, network, Traefik labels.
2. Generar `infra/dokploy/zitadel/compose.yml`:
   ```yaml
   # Zitadel Teslita IdP — reference compose (Wave A retro-docs)
   # NOT deployed directly — Dokploy manages the live deploy.
   # Source of truth for disaster recovery and reproduction.
   # Generated 2026-04-18 from live state in VPS turismo.
   version: "3.9"

   x-zitadel-env: &zitadel-env
     ZITADEL_MASTERKEY: ${ZITADEL_MASTERKEY}
     ZITADEL_EXTERNALDOMAIN: ${ZITADEL_EXTERNALDOMAIN}
     ZITADEL_EXTERNALSECURE: ${ZITADEL_EXTERNALSECURE}
     ZITADEL_EXTERNALPORT: ${ZITADEL_EXTERNALPORT}
     ZITADEL_DATABASE_POSTGRES_HOST: ${ZITADEL_DATABASE_POSTGRES_HOST}
     ZITADEL_DATABASE_POSTGRES_PORT: ${ZITADEL_DATABASE_POSTGRES_PORT}
     ZITADEL_DATABASE_POSTGRES_DATABASE: ${ZITADEL_DATABASE_POSTGRES_DATABASE}
     ZITADEL_DATABASE_POSTGRES_USER_USERNAME: ${ZITADEL_DATABASE_POSTGRES_USER}
     ZITADEL_DATABASE_POSTGRES_USER_PASSWORD: ${ZITADEL_DATABASE_POSTGRES_PASSWORD}
     ZITADEL_DATABASE_POSTGRES_USER_SSL_MODE: disable
     ZITADEL_DATABASE_POSTGRES_ADMIN_USERNAME: ${ZITADEL_DATABASE_POSTGRES_USER}
     ZITADEL_DATABASE_POSTGRES_ADMIN_PASSWORD: ${ZITADEL_DATABASE_POSTGRES_PASSWORD}
     ZITADEL_DATABASE_POSTGRES_ADMIN_SSL_MODE: disable
     ZITADEL_FIRSTINSTANCE_ORG_NAME: nuestrascuentitas
     ZITADEL_EVENTS_MAXAGE: 17520h  # 2 años retention
     ZITADEL_SMTP_*: <see env.example>

   services:
     zitadel:
       image: ghcr.io/zitadel/zitadel:${ZITADEL_VERSION}  # pinned, no :latest
       restart: unless-stopped
       command: start-from-init --masterkey "${ZITADEL_MASTERKEY}" --tlsMode external
       environment: *zitadel-env
       depends_on:
         postgres:
           condition: service_healthy
       networks:
         - zitadel-net
       labels:
         # Traefik Dokploy labels — ver infra/dokploy/zitadel/traefik.labels.md
         - traefik.enable=true
         - traefik.http.routers.zitadel.rule=Host(`id.nuestrascuentitas.com`)
         - traefik.http.routers.zitadel.entrypoints=websecure
         - traefik.http.routers.zitadel.tls.certresolver=letsencrypt
         - traefik.http.services.zitadel.loadbalancer.server.port=8080
         - traefik.http.services.zitadel.loadbalancer.server.scheme=h2c

     postgres:
       image: postgres:${ZITADEL_POSTGRES_VERSION}
       restart: unless-stopped
       environment:
         POSTGRES_USER: ${ZITADEL_DATABASE_POSTGRES_USER}
         POSTGRES_PASSWORD: ${ZITADEL_DATABASE_POSTGRES_PASSWORD}
         POSTGRES_DB: ${ZITADEL_DATABASE_POSTGRES_DATABASE}
       volumes:
         - zitadel-postgres-data:/var/lib/postgresql/data
       networks:
         - zitadel-net
       healthcheck:
         test: ["CMD-SHELL", "pg_isready -U ${ZITADEL_DATABASE_POSTGRES_USER} -d ${ZITADEL_DATABASE_POSTGRES_DATABASE}"]
         interval: 10s
         timeout: 5s
         retries: 5

   volumes:
     zitadel-postgres-data:
       external: true
       name: ${ZITADEL_POSTGRES_VOLUME_NAME}

   networks:
     zitadel-net:
       external: true
       name: ${ZITADEL_DOCKER_NETWORK_NAME}
   ```
3. Generar `infra/dokploy/zitadel/env.example`:
   ```
   # Zitadel Teslita env vars (reference)
   # All values live in Infisical vault teslita, project bitacora, env prod.
   # Pull with: mkey pull bitacora prod

   ZITADEL_VERSION=<pinned from T1.1>
   ZITADEL_POSTGRES_VERSION=<pinned from T1.1>
   ZITADEL_POSTGRES_VOLUME_NAME=<real volume name from T1.1>
   ZITADEL_DOCKER_NETWORK_NAME=<real network name from T1.1>

   # Core
   ZITADEL_MASTERKEY=             # NEVER rotate without re-encrypting DB
   ZITADEL_EXTERNALDOMAIN=id.nuestrascuentitas.com
   ZITADEL_EXTERNALSECURE=true
   ZITADEL_EXTERNALPORT=443

   # DB
   ZITADEL_DATABASE_POSTGRES_HOST=postgres
   ZITADEL_DATABASE_POSTGRES_PORT=5432
   ZITADEL_DATABASE_POSTGRES_DATABASE=zitadel
   ZITADEL_DATABASE_POSTGRES_USER=zitadel
   ZITADEL_DATABASE_POSTGRES_PASSWORD=

   # SMTP (Resend)
   ZITADEL_SMTP_HOST=smtp.resend.com
   ZITADEL_SMTP_PORT=587
   ZITADEL_SMTP_USER=resend
   ZITADEL_SMTP_PASSWORD=              # = RESEND_API_KEY_TESLITA
   ZITADEL_SMTP_SENDER_EMAIL=noreply@nuestrascuentitas.com
   ZITADEL_SMTP_SENDER_NAME="Zitadel Teslita"

   # Admin (T2.3)
   ZITADEL_ADMIN_EMAIL=admin@nuestrascuentitas.com
   ZITADEL_ADMIN_PASSWORD=
   ZITADEL_ADMIN_PAT=
   ZITADEL_ADMIN_TOTP_RECOVERY=

   # OAuth Clients (T2.4)
   ZITADEL_PROJECT_BITACORA_ID=
   ZITADEL_CLIENT_BITACORA_WEB_ID=
   ZITADEL_CLIENT_BITACORA_API_ID=
   ZITADEL_CLIENT_BITACORA_API_SECRET=
   # ... (repeat per org)

   # Backup (T3.1)
   ZITADEL_BACKUP_OFFSITE_PROVIDER=b2
   ZITADEL_BACKUP_OFFSITE_BUCKET=
   ZITADEL_BACKUP_OFFSITE_ACCESS_KEY=
   ZITADEL_BACKUP_OFFSITE_SECRET_KEY=
   ```
4. Generar `infra/dokploy/zitadel/traefik.labels.md` — documentar los labels Traefik reales del runtime descubiertos en T1.1.
5. Generar `infra/dokploy/zitadel/README.md` — intro a la carpeta, warnings (NO deploy directo, referencia solamente), link a runbooks (T3.4).
6. Validar compose: `docker compose -f infra/dokploy/zitadel/compose.yml config > /dev/null` (requiere env vars pero al menos detecta YAML errors).
7. Persistir reporte y commit.

## Skeleton (reporte)
```md
# T3.3 — Compose retro git-tracked

## Archivos creados
- infra/dokploy/zitadel/compose.yml: yes
- infra/dokploy/zitadel/env.example: yes
- infra/dokploy/zitadel/traefik.labels.md: yes
- infra/dokploy/zitadel/README.md: yes

## Version pinneada
- Zitadel: `v<x.y.z>`
- Postgres: `<version>`

## Validation
- docker compose config: OK (sin errores YAML)
```

## Verify
`test -f infra/dokploy/zitadel/compose.yml && test -f infra/dokploy/zitadel/env.example && test -f infra/dokploy/zitadel/README.md` exits 0.

## Commit
`feat(infra): T3.3 retro-gen zitadel compose + env.example + traefik labels`
