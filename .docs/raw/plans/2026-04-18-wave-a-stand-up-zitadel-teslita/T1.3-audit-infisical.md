# Task T1.3: Audit Infisical secrets existentes (vault teslita, proj bitacora)

## Shared Context
**Goal:** listar que keys ZITADEL_*, DOKPLOY_*, ADMIN_* ya existen en vault Infisical `teslita` project `bitacora` env `prod`. Identificar gaps para T3.2.
**Stack:** `mkey` CLI + Infisical REST via `teslita` vault.
**Architecture:** La skill `mi-key-cli` es la interfaz oficial. Vault URL `http://100.115.71.27:9881` (accesible via Tailscale).

## Locked Decisions
- Usar solo `mkey` (no `infisical` CLI directo) para consistencia y fallback chain de auth.
- Read-only. Solo listar keys (nombres), NUNCA volcar valores al reporte.
- Evidencia en `.docs/raw/reports/2026-04-18-wave-a-audit/T1.3-infisical-audit.md`.

## Task Metadata
```yaml
id: T1.3
depends_on: [T0.1]
agent_type: ps-explorer
files:
  - create: .docs/raw/reports/2026-04-18-wave-a-audit/T1.3-infisical-audit.md
complexity: low
done_when: "Reporte T1.3 persistido con lista de keys existentes vs requeridas, gaps identificados"
```

## Reference
- `$HOME/.claude/skills/mi-key-cli/SKILL.md` — comandos pull/status
- Memoria `feedback_env_vars_infisical.md` — todo secret debe vivir en Infisical

## Prompt
1. Correr `bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh pull bitacora prod --stdout > /tmp/secrets.env 2>&1`. Verificar exit code 0.
2. Listar solo **nombres** (NO valores): `awk -F= '/^[A-Z_]+=/{print $1}' /tmp/secrets.env | sort -u > /tmp/keys.txt`.
3. Cross-check con lista de keys requeridas para Zitadel stack:
   ```
   # Dokploy
   DOKPLOY_API_KEY
   DOKPLOY_URL
   DOKPLOY_ENVIRONMENT_ID
   # Zitadel runtime
   ZITADEL_MASTERKEY
   ZITADEL_EXTERNALDOMAIN
   ZITADEL_EXTERNALSECURE
   ZITADEL_DATABASE_POSTGRES_HOST
   ZITADEL_DATABASE_POSTGRES_PORT
   ZITADEL_DATABASE_POSTGRES_DATABASE
   ZITADEL_DATABASE_POSTGRES_USER
   ZITADEL_DATABASE_POSTGRES_PASSWORD
   # Zitadel admin
   ZITADEL_ADMIN_PAT
   ZITADEL_ADMIN_EMAIL
   ZITADEL_ADMIN_INITIAL_PASSWORD
   # Zitadel SMTP (Resend)
   ZITADEL_SMTP_HOST
   ZITADEL_SMTP_PORT
   ZITADEL_SMTP_USER
   ZITADEL_SMTP_PASSWORD
   ZITADEL_SMTP_SENDER_EMAIL
   ZITADEL_SMTP_SENDER_NAME
   # Zitadel postgres backup offsite (T3.1)
   ZITADEL_BACKUP_OFFSITE_URL      # e.g. s3://..., b2://..., or rsync target
   ZITADEL_BACKUP_OFFSITE_SECRET   # API key or equivalent
   # Resend compartido teslita (YA existe segun infra/.env)
   RESEND_API_KEY_TESLITA
   RESEND_SENDER_EMAIL
   RESEND_SMTP_HOST
   RESEND_SMTP_PORT
   RESEND_SMTP_USER
   ```
4. Matrizar: por cada key requerida, marcar `presente / ausente`.
5. Persistir reporte con skeleton abajo.

## Execution Procedure
1. `bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh pull bitacora prod --stdout > /tmp/secrets.env`.
2. Si el comando falla con `InfisicalProfileSwitchFailed`: stop, escalar T0.1 sub-step 2 no quedo cerrado.
3. Extraer solo nombres de keys, generar cross-check table.
4. Persistir reporte + commit.

## Skeleton
```md
# T1.3 — Infisical audit (2026-04-18)

## Listado crudo de keys
<count total>: <N>
<primeras 20 keys sort -u>

## Cross-check con keys requeridas Wave A
| Key | Presente en Infisical? | Fuente en T3.2 |
|-----|------------------------|----------------|
| DOKPLOY_API_KEY | yes (seteada en T0.1) | - |
| DOKPLOY_URL | yes | - |
| ZITADEL_MASTERKEY | <yes/no> | T1.1 Dokploy env |
| ZITADEL_DATABASE_POSTGRES_PASSWORD | <yes/no> | T1.1 Dokploy env |
| ZITADEL_ADMIN_PAT | yes (seteada en T0.1) | - |
| ZITADEL_SMTP_* | <yes/no> | `RESEND_*` existentes + T2.2 |
| ZITADEL_BACKUP_OFFSITE_* | <yes/no> | T3.1 |

## Gaps (keys a agregar en T3.2)
- <lista>

## Keys en Infisical que NO estan en infra/.env local
- <diff lista>

## Recomendacion
- Correr `mkey pull bitacora prod` para alinear `infra/.env` local con Infisical antes de arrancar Wave 2.
```

## Verify
`test -s .docs/raw/reports/2026-04-18-wave-a-audit/T1.3-infisical-audit.md && ! grep -E "=[^<*]" .docs/raw/reports/2026-04-18-wave-a-audit/T1.3-infisical-audit.md` — el segundo grep confirma que no haya valores en claro (busca `KEY=valor` no enmascarado).

## Commit
`docs(report): T1.3 infisical secrets audit`
