# Task T3.2: Persistir todos los secrets Zitadel en Infisical

## Shared Context
**Goal:** migrar a Infisical (via mkey) todos los secrets de Zitadel descubiertos en T1.1 que aun no esten ahi, dejando Dokploy env apuntando a Infisical (via sync o copia estatica).
**Stack:** `mkey set bitacora <KEY> <VALUE> --env prod`.
**Architecture:** policy del proyecto: todo secret debe vivir en Infisical. Dokploy env vars son duplicados locales que se deben mantener sincronizados.

## Locked Decisions
- Prefijo `ZITADEL_` para todas las keys relacionadas.
- Valores se leen de T1.1 report (no se regeneran).
- No tocar masterkey — solo copiarlo a Infisical. Nunca regenerar.
- Para cada key: set + verify con `mkey pull` + commit encrypted backup.

## Task Metadata
```yaml
id: T3.2
depends_on: [T2.5]
agent_type: ps-worker
files:
  - modify: infra/secrets.enc.env  # SOPS encrypted backup auto-regenerated
  - create: .docs/raw/reports/2026-04-18-wave-a-audit/T3.2-secrets.md
complexity: medium
done_when: "Todas las keys del target list aparecen en mkey pull bitacora prod --stdout con valores no vacios; infra/secrets.enc.env commiteado"
```

## Reference
- T1.1 report — env vars Zitadel reales (masked en report, valores reales solo en Dokploy)
- T1.3 report — gap de keys
- Memoria `feedback_env_vars_infisical.md`

## Prompt
1. Obtener valores reales de Dokploy env var por var via `dkp.sh status <appId>`. Para cada key del target list, extraer el valor actual.
2. Persistir en Infisical con `mkey set`:
   ```bash
   $LASTEXITCODE = 0  # si se usa pwsh, workaround strict mode
   # desde bash:
   bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora ZITADEL_MASTERKEY "<value>" --env prod
   bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora ZITADEL_EXTERNALDOMAIN "id.nuestrascuentitas.com" --env prod
   bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora ZITADEL_EXTERNALSECURE "true" --env prod
   bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora ZITADEL_EXTERNALPORT "443" --env prod
   bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora ZITADEL_DATABASE_POSTGRES_HOST "<host>" --env prod
   bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora ZITADEL_DATABASE_POSTGRES_PORT "5432" --env prod
   bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora ZITADEL_DATABASE_POSTGRES_DATABASE "zitadel" --env prod
   bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora ZITADEL_DATABASE_POSTGRES_USER "zitadel" --env prod
   bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora ZITADEL_DATABASE_POSTGRES_PASSWORD "<pass>" --env prod
   bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora ZITADEL_VERSION "v<x.y.z>" --env prod
   bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora ZITADEL_POSTGRES_VOLUME_NAME "<vol>" --env prod
   bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora ZITADEL_POSTGRES_CONTAINER_NAME "<container>" --env prod
   bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora ZITADEL_DOKPLOY_APP_ID_WEB "<appId>" --env prod
   bash $HOME/.claude/skills/mi-key-cli/scripts/mkey.sh set bitacora ZITADEL_DOKPLOY_APP_ID_POSTGRES "<appId>" --env prod
   ```
3. Verificar con `mkey pull bitacora prod --stdout | awk -F= '/^ZITADEL_/{print $1}' | sort -u` cada clave.
4. Correr `mkey backup bitacora` para actualizar `infra/secrets.enc.env` y commitearlo:
   ```
   git add infra/secrets.enc.env
   git commit -m "feat(secrets): T3.2 migrate zitadel secrets to infisical via mkey"
   ```
5. Persistir reporte.

## Skeleton
```md
# T3.2 — Secrets en Infisical

## Keys migradas
- ZITADEL_MASTERKEY: yes
- ZITADEL_DATABASE_POSTGRES_*: 5 keys yes
- ZITADEL_EXTERNAL*: 3 keys yes
- ZITADEL_ADMIN_*: 5 keys yes (T0.1+T2.3)
- ZITADEL_SMTP_*: 6 keys yes (T2.2)
- ZITADEL_CLIENT_*: 12 keys yes (T2.4)
- ZITADEL_BACKUP_OFFSITE_*: 5 keys yes (T3.1)
- ZITADEL_PROJECT_*: 4 keys yes (T2.4)
- ZITADEL_DOKPLOY_APP_ID_*: 2 keys yes
- ZITADEL_VERSION: yes

## Total: N keys ZITADEL_ en Infisical

## Backup encrypted
- infra/secrets.enc.env actualizado: yes
- Commit hash: `<sha>`
```

## Verify
`mkey pull bitacora prod --stdout | awk -F= '/^ZITADEL_/{print $1}' | sort -u | wc -l` >= 25.

## Commit
`feat(secrets): T3.2 migrate zitadel secrets to infisical via mkey`
