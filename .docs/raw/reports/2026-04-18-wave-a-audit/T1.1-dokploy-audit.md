# T1.1 — Dokploy audit Zitadel Teslita (2026-04-18)

**Fecha:** 2026-04-18 evening (ART)
**Auditor:** Claude Code (Wave A plan `2026-04-18-wave-a-stand-up-zitadel-teslita`)
**Scope:** inventario Dokploy del stack Zitadel live en `id.nuestrascuentitas.com`

## Resumen ejecutivo

Stack Zitadel deployado por Dokploy en VPS turismo (`54.37.157.93`) el **2026-04-18 00:22 UTC** (hace <24h). Admin bootstrap con credenciales en env vars (recuperables). Version `v4.9.0` (major 4, nueva — NO v2.x como asumía el prompt original). Hay 2 Postgres en el stack: una activa (`pg17`) y una legacy (creada 7 min antes, probablemente de prueba de migracion).

## Proyecto Dokploy

| Campo | Valor |
|-------|-------|
| Project name | `teslita-shared-idp` |
| Project ID | `AKHsAJScexTwhJBzfFRlk` |
| Description | "Shared Teslita Zitadel issuer on turismo" |
| Environment | `production` |

## Zitadel webapp

| Campo | Valor |
|-------|-------|
| name | `teslita-shared-zitadel` |
| applicationId | `zFdEECmPr1hhxwL0DKu4B` |
| appName (Swarm) | `app-program-optical-matrix-xmjswe` |
| sourceType | `docker` |
| dockerImage | `ghcr.io/zitadel/zitadel:v4.9.0` (pinned, no :latest) |
| command | `/app/zitadel start-from-init --masterkeyFromEnv --tlsMode external` |
| applicationStatus | `done` |
| healthCheckSwarm | None |
| Dominio | `id.nuestrascuentitas.com` (Traefik port 8080 → TLS termina en Traefik) |
| certType | `letsencrypt` (autoemitido) |
| HTTPS | `true` |

**Version drift check:** OK — image tag `v4.9.0` pinneada explícita. NO `:latest`. ✓

## Postgres stack (2 instancias)

### ACTIVA — `teslita-shared-idp-db-pg17`
| Campo | Valor |
|-------|-------|
| postgresId | `BjjSOBWAwe6XpGttK4XoY` |
| appName (Swarm) | `postgres-reboot-wireless-panel-chhbwg` |
| Database name | `shared_zitadel_pg17` |
| User | `zitadel` |
| createdAt | `2026-04-18T00:27:32 UTC` |
| applicationStatus | `done` |

**Confirmacion uso:** env `ZITADEL_DATABASE_POSTGRES_HOST=postgres-reboot-wireless-panel-chhbwg` en webapp apunta aqui. ✓

### LEGACY — `teslita-shared-idp-db`
| Campo | Valor |
|-------|-------|
| postgresId | `5e5bKMG5jBS30S7d8nn6c` |
| appName (Swarm) | `postgres-bypass-wireless-bus-tupzoj` |
| createdAt | `2026-04-18T00:20:24 UTC` (7 min antes de la activa) |
| applicationStatus | `done` |

**Decision pendiente:** eliminar esta PG legacy (usada probablemente para prueba inicial pg16 antes de migrar a pg17). Si no tiene datos productivos, se puede remover para liberar disco y evitar confusion. **Flag para T2 o T3.1.**

**Aislamiento:** ambas Postgres son dedicadas al stack Zitadel — NO comparten con Bitacora (`postgres-reboot-solid-state-application-l55mww`). ✓ Decision lockeada cumplida.

## Historico deployments

| Fecha UTC | Deployment | Status |
|-----------|-----------|--------|
| 2026-04-18T00:22:27 | Initial shared ZITADEL core deploy | done |
| 2026-04-18T00:23:39 | Rebuild | done |
| 2026-04-18T00:24:28 | Rebuild | done |
| 2026-04-18T00:25:53 | Rebuild | done |
| 2026-04-18T00:27:59 | Rebuild | done |

Stack deployado hace <24h. No hay data productiva significativa aun probablemente.

## Env vars (masked) — webapp

Keys descubiertas:
- `ZITADEL_MASTERKEY` (32 chars) — persistida en Infisical ✓
- `ZITADEL_EXTERNALDOMAIN` = `id.nuestrascuentitas.com`
- `ZITADEL_EXTERNALPORT` = `443`
- `ZITADEL_EXTERNALSECURE` = `true`
- `ZITADEL_TLS_ENABLED` = `false` (Traefik termina TLS)
- `ZITADEL_DATABASE_POSTGRES_HOST` = `postgres-reboot-wireless-panel-chhbwg`
- `ZITADEL_DATABASE_POSTGRES_PORT` = `5432`
- `ZITADEL_DATABASE_POSTGRES_DATABASE` = `shared_zitadel_pg17`
- `ZITADEL_DATABASE_POSTGRES_USER_USERNAME` = `zitadel`
- `ZITADEL_DATABASE_POSTGRES_USER_PASSWORD` (32 chars)
- `ZITADEL_DATABASE_POSTGRES_USER_SSL_MODE` = `disable`
- `ZITADEL_DATABASE_POSTGRES_ADMIN_USERNAME` = `zitadel`
- `ZITADEL_DATABASE_POSTGRES_ADMIN_PASSWORD` (32 chars)
- `ZITADEL_DATABASE_POSTGRES_ADMIN_SSL_MODE` = `disable`
- `ZITADEL_DATABASE_POSTGRES_ADMIN_EXISTINGDATABASE` = `shared_zitadel_pg17`
- `ZITADEL_FIRSTINSTANCE_ORG_NAME` = `Nuestras Cuentitas`
- `ZITADEL_FIRSTINSTANCE_ORG_HUMAN_USERNAME` = `iam-admin@nuestrascuentitas.com`
- `ZITADEL_FIRSTINSTANCE_ORG_HUMAN_PASSWORD` (24 chars)

Todas estas keys + metadata Dokploy (projectId, appIds) persistidas en Infisical `teslita` proj `bitacora` env `prod` via `mkey set`. ✓

## Hallazgos criticos

1. ✅ **Admin recuperable.** El password inicial del admin `iam-admin@nuestrascuentitas.com` esta en env var `ZITADEL_FIRSTINSTANCE_ORG_HUMAN_PASSWORD` (persistido a Infisical como `ZITADEL_ADMIN_INITIAL_PASSWORD`). **Permite login admin inmediato via UI.**

2. ⚠️ **Version v4.9.0** (no v2.x). El plan original asumia v2.x. Endpoints mgmt API cambian — necesitamos consultar docs Zitadel v4. Impacto: T1.2 (audit mgmt API) y T2.1-T2.5 (mutaciones) pueden tener endpoints ligeramente diferentes.

3. ⚠️ **Org bootstrap "Nuestras Cuentitas"** (con espacios + capitalizacion). La decision lockeada decia org name literal `nuestrascuentitas`. Opciones: (a) renombrar en T2.1, (b) adoptar el nombre actual y ajustar decision. Recomendacion: (a) para consistencia con decision original.

4. ⚠️ **2 Postgres instancias** (legacy + activa). La legacy (`-tupzoj`) posiblemente vacía/prueba. Decision: validar que esta vacía y eliminarla post-audit para liberar recursos.

5. ✅ **Version pinneada** y **Postgres dedicada** — decisiones lockeadas cumplidas.

6. ❌ **No hay SMTP wireado aun** — no hay env vars `ZITADEL_SMTP_*` en el runtime. Wireado Resend pendiente (T2.2).

7. ❌ **MFA no aplicada** — no hay policies custom detectadas en env vars (son defaults). T2.5 debe aplicarlas.

8. ❌ **No hay backup job** — no hay referencia a cron o script de backup en el stack. T3.1 debe crearlo.

9. ⚠️ **No hay user PAT emitido aun** — necesario para T1.2 audit mgmt API. Proximo paso: loguear admin via UI y emitir PAT.

## Proximos pasos

- [ ] **T0.1 sub-step 3 (Zitadel admin access):** user loguea en `https://id.nuestrascuentitas.com/ui/console` con `iam-admin@nuestrascuentitas.com` + password (de Infisical `ZITADEL_ADMIN_INITIAL_PASSWORD`). Emite PAT con scopes admin. Me lo pasa. Lo guardo como `ZITADEL_ADMIN_PAT`.
- [ ] T1.2 (audit mgmt API) una vez con PAT: listar orgs, clients, users, policies, SMTP state en la instancia v4.9.0.
- [ ] Decidir fate Postgres legacy post-T1.2.
- [ ] Decidir rename org "Nuestras Cuentitas" → `nuestrascuentitas` en T2.1.
